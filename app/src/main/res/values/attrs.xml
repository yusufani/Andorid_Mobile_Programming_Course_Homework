<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ds">
        <attr name="backgroundcolor" format="color" />
        <attr name="cardbackground" format="color" />
        <attr name="textcolor" format="color" />
        <attr name="tintcolor" format="color"></attr>
        <attr name="buttoncolor" format="color"></attr>
        <attr name="buttontint" format="color"></attr>
        <attr name="texthint" format="color"></attr>
        <attr name="textColorHint" format="color"></attr>
    </declare-styleable>

</resources>
